---
- name: restart wireguard
  service:
    name: wg-quick@{{ vps_wg_interface }}
    enabled: yes
    state: restarted

- name: save iptables
  command: /usr/sbin/netfilter-persistent save

- name: restart iptables
  service:
    name: netfilter-persistent
    state: restarted
  listen: save iptables

- name: execute antifilter
  command: "{{ vps_antifilter_path }}"
  listen: restart bird

- name: restart bird
  service:
    name: bird
    state: restarted
